# 프로필 카드 게이미피케이션 — 디자인 스펙

> 날짜: 2026-03-19
> 상태: 확정

---

## 1. 목표

프로필 페이지의 ProfileCard에 헥사곤 레이더 차트 + 실측 스탯을 통합하여, 피파/FM 스타일의 "선수 카드" 느낌을 주는 게이미피케이션 요소를 추가한다. 프로필을 열었을 때 첫인상에서 즉각적인 시각적 임팩트를 제공한다.

## 2. 핵심 결정 사항

| 항목 | 결정 |
|------|------|
| 스탯 표현 방식 | 헥사곤 레이더 차트 (FM 스타일) |
| 데이터 소스 | 실측 데이터 (50m, 셔틀런, 키, 몸무게 등) |
| 배치 위치 | ProfileCard 내부에 통합 (하나의 카드 단위) |
| 레이아웃 | 레이더(좌) + 스탯 리스트(우) 사이드 바이 사이드 |
| 카드 스타일 | Lv.4 에이스 (강한 골드 글로우) — 레벨 분기 없음 |
| 종합 점수 | 없음 (피파와 차별화) |
| 법적 안전 | 독자 디자인 (라운드 직사각형, 자체 색상/약어/구조) |

## 3. 카드 구조

```
┌──────────────────────────────────────┐
│ [골드 conic-gradient 보더 글로우]     │
│ ┌──────────────────────────────────┐ │
│ │ FW          [⭐ Lv.4] [MVP ×3]  │ │  ← 포지션(좌상) + 레벨/MVP(우상)
│ │ 포워드                           │ │
│ │                                  │ │
│ │      (선수 사진 영역 230px)       │ │  ← 배경: radial gradient + pitch 패턴
│ │                                  │ │
│ │ 배준혁                           │ │  ← 이름 (26px, 900, 사진 위 오버레이)
│ │ @junhyuk · 부산IP U-15 · 2011   │ │  ← 핸들 + 팀 + 출생
│ │ [168cm] [52kg] [오른발]          │ │  ← 신체정보 필 뱃지 (반투명 배경)
│ ├──── ═══════ 골드 디바이더 ═══════ ┤ │  ← shimmer 애니메이션
│ │                                  │ │
│ │  ┌────────┐  속도   7.2초  ▲0.3  │ │
│ │  │ 헥사곤  │  민첩   8.1초  ▲0.5  │ │
│ │  │ 레이더  │  지구력  45회   —    │ │  ← 레이더(130px) + 스탯 리스트(우)
│ │  │ 차트    │  파워   52kg  ▲3    │ │
│ │  │ 130px  │  유연   38cm  ▲2    │ │
│ │  └────────┘  체격   168cm ▲2    │ │
│ │                                  │ │
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│    127     │      84     │    1.2k   │  ← 소셜 카드 (분리, 세로 구분선)
│   팔로워    │    팔로잉    │    조회    │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│  [🎬 영상]  [📋 기록]                │  ← 기존 탭 바
└──────────────────────────────────────┘
```

## 4. 디자인 상세

### 4.1 카드 보더

- `conic-gradient` 골드 보더 (2px, 각도별 밀도 차이)
- `box-shadow: 0 0 50px rgba(212,168,83,0.05)`
- `inset 0 1px 0 rgba(245,215,142,0.08)` 상단 하이라이트
- 필름 그레인 오버레이 (fractalNoise SVG, mix-blend-mode: overlay)

### 4.2 사진 영역 (230px)

- 배경: `radial-gradient` 골드 하이라이트 + pitch line 패턴 (10px 간격)
- 하단 80% cinematic fade (#0A0A0C로 페이드)
- 이름/메타/신체정보는 사진 위 오버레이 (z-index: 3)

### 4.3 포지션/레벨/MVP 뱃지

- 포지션 (좌상): Oswald 32px bold, 골드, text-shadow 글로우
- 레벨 (우상): pill 형태, backdrop-blur, 골드 보더
- MVP (우상 레벨 아래): Rajdhani 8px, 골드, 작은 뱃지

### 4.4 이름 영역

- 이름: Noto Sans KR 26px 900 weight
- 메타: 11px, handle(#A1A1AA) · 팀 · 출생 (dot 구분)
- 신체정보: 반투명 pill 뱃지 (`rgba(255,255,255,0.06)`, backdrop-blur)
  - 168cm / 52kg / 오른발

### 4.5 골드 디바이더

- 1px 골드 gradient 라인
- 중앙 48px 두꺼운 accent bar (7색 골드 gradient + box-shadow)
- shimmer 애니메이션 (4초 주기)

### 4.6 스탯 섹션

**레이더 차트 (좌, 130px)**
- 6각형 3단 그리드 + 축선
- 데이터 폴리곤: radialGradient 채움 + 골드 stroke 1.3px
- 꼭짓점: 3px 골드 도트 + 6px 링 글로우
- 라벨: 한국어 (속도/민첩/지구력/파워/유연/체격)
- 진입 애니메이션: scale 0.75 → 1 + rotate -8° → 0° (0.9s)

**스탯 리스트 (우)**
- 6행, padding 3.5px (컴팩트)
- 라벨: Rajdhani 10px, #52525B
- 수치: Oswald 16px, #FAFAFA
- 단위: Rajdhani 8px, #3F3F46
- 변화량: ▲ #4ADE80 (향상) / — #27272A (변동 없음)
- 진입 애니메이션: slideR (0.35s, 순차 delay 0.05s)

### 4.7 소셜 카드 (분리)

- 카드 밖 독립 영역 (margin-top: 10px)
- `background: #0E0E12`, `border: 1px solid rgba(255,255,255,0.04)`
- 3열: 팔로워 | 팔로잉 | 조회
- 항목 간 세로 구분선 (`1px solid rgba(255,255,255,0.06)`)
- 숫자: Oswald 16px, 라벨: 9px #52525B

### 4.8 레이더 차트 데이터 매핑

| 축 | 한국어 | 데이터 소스 | 단위 |
|----|--------|------------|------|
| SPD → 속도 | 속도 | 50m 스프린트 | 초 |
| AGI → 민첩 | 민첩 | 셔틀런 | 초 |
| STM → 지구력 | 지구력 | 왕복달리기/비프테스트 | 회 |
| PWR → 파워 | 파워 | 제자리멀리뛰기/킥력 | kg/cm |
| FLX → 유연 | 유연 | 앉아윗몸앞으로굽히기 | cm |
| HGT → 체격 | 체격 | 키 | cm |

- 레이더 값은 동일 연령대 백분위(0~100)로 정규화하여 표시
- 측정 데이터가 없는 축은 중앙(0)으로 표시 + "미측정" 표기

## 5. 변경 범위

### 수정 파일

| 파일 | 변경 내용 |
|------|----------|
| `src/components/player/ProfileCard.tsx` | 레이더+스탯 섹션 추가, 신체정보 필 뱃지 이동, 소셜 바 분리 |
| `src/components/player/ProfileRadar.tsx` | **신규** — SVG 헥사곤 레이더 차트 컴포넌트 |
| `src/components/player/ProfileStatList.tsx` | **신규** — 스탯 리스트 컴포넌트 (6행) |
| `src/components/player/SocialCard.tsx` | **신규** — 분리된 소셜 지표 카드 |
| `src/lib/radar-calc.ts` | 레이더 정규화 로직 수정 (백분위 기반) |
| `src/styles/globals.css` | shimmer 애니메이션, 카드 글로우 변수 추가 |
| `src/app/(main)/profile/page.tsx` | ProfileCard → SocialCard 순서 변경 |

### 재활용 가능

- `src/components/player/RadarChart.tsx` — 기존 InfoTab의 레이더 로직 참조
- `src/hooks/useStats.ts` — 측정 데이터 fetch 로직

## 6. 기존 기능과의 관계

- InfoTab의 RadarChart는 유지 (기록 탭에서 상세 보기용)
- ProfileCard 내 레이더는 "요약 미리보기" 역할 (탭 전환 없이 첫인상)
- 기존 ProfileCard의 아바타, 이름, 포지션 뱃지, 레벨 뱃지는 그대로 유지
- 신체정보(키/몸무게/주발)는 기존 위치에서 이름 아래 pill 형태로 이동

## 7. 목업 참조

`/.superpowers/brainstorm/68827-1773924399/card-final-v4.html`
